type Query {
  hello: String
  myInterfaceById(id: ID!): MyInterface @lookup
}

interface MyInterface @key(fields: "id") {
  id: ID!
  field: String
}

type MyType implements MyInterface @key(fields: "id") {
  id: ID!
  field: String
}
